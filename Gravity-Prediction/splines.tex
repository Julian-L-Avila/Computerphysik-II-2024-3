\documentclass{standalone}
\usepackage{tikz}
\usepackage{pgfplots}
\usepackage{siunitx}
\usepackage[dvipsnames]{xcolor}

\pgfplotsset{compat=1.18}

\begin{document}

\begin{tikzpicture}
	\begin{axis}[
		title={Splines \qty{2.0}{\metre}},
		width=12cm,
		height=8cm,
		xlabel={\(t \, [\si{\second}]\)},
		ylabel={\(x \, [\si{\metre}]\)},
		grid=major,
		grid style=dashed,
		legend style={font=\small},
		legend pos=north east,
		every axis plot/.append style={thick},
		xmin=0,
		xmax=0.7,
		xtick distance=0.1,
	]
	\addplot[
		only marks,
		color=Plum,
		line width=1.2pt,
		mark=*,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=1]{./Splines/2m-data-splines.tsv};
	\addlegendentry{Data}

	\addplot[
		only marks,
		color=Plum,
		mark=square*,
		mark options={scale=1.5},
		each nth point=10,
		forget plot,
	]
	table[col sep=tab, x index=0, y index=1]{./Splines/2m-data-splines.tsv};

	\addplot[
		color=Green,
		line width=1.2pt,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=2]{./Splines/2m-data-splines.tsv};
	\addlegendentry{Quadratic}

	\addplot[
		color=Cyan,
		line width=1.2pt,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=3]{./Splines/2m-data-splines.tsv};
	\addlegendentry{Cubic}
	\end{axis}
\end{tikzpicture}

\begin{tikzpicture}
	\begin{axis}[
		title={Splines \qty{1.0}{\metre}},
		width=12cm,
		height=8cm,
		xlabel={\(t \, [\si{\second}]\)},
		ylabel={\(x \, [\si{\metre}]\)},
		grid=major,
		grid style=dashed,
		legend style={font=\small},
		legend pos=north east,
		every axis plot/.append style={thick},
		xmin=0,
		xmax=0.5,
		xtick distance=0.1,
	]
	\addplot[
		only marks,
		color=Plum,
		line width=1.2pt,
		mark=*,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=1]{./Splines/1m-data-splines.tsv};
	\addlegendentry{Data}

	\addplot[
		only marks,
		color=Plum,
		mark=square*,
		mark options={scale=1.5},
		each nth point=7,
		forget plot,
	]
	table[col sep=tab, x index=0, y index=1]{./Splines/1m-data-splines.tsv};

	\addplot[
		color=Green,
		line width=1.2pt,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=2]{./Splines/1m-data-splines.tsv};
	\addlegendentry{Quadratic}

	\addplot[
		color=Cyan,
		line width=1.2pt,
		mark options={scale=0.5},
	]
	table[col sep=tab, x index=0, y index=3]{./Splines/1m-data-splines.tsv};
	\addlegendentry{Cubic}
	\end{axis}
\end{tikzpicture}

\end{document}

